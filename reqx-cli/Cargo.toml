[package]
name = "reqx-cli"
version = "0.1.0"
edition = "2021"
description = "CLI interpreter for reqx DSL"

[dependencies]
reqwest = { version = "0.12", features = ["blocking"] }
clap = { version = "4", features = ["derive"] }
reqx-core = { path = "../reqx-core" }
colored = "2"
